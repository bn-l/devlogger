- 1 | 2026-04-14 13:00:49: Capped `list` output at 80 chars with a ` (...N more)` suffix; added `sections` command listing section dirs under DEVLOG/ alphabetically (only those with the canonical devlog file, so stray dirs are ignored). Added truncate_line helper on Entry; new unit tests (entry_truncation), cli-parse tests (sections_parse), integration tests (sections_flow + list truncation); fixed edge_cases::very_long_entry_text_is_accepted to assert via `read` since `list` now truncates.
