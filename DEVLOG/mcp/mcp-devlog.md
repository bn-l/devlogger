- 1 | 2026-04-17 12:56:28: Two registration/handshake tests assert server info.instructions mentions every tool name. Fleshed-out instructions had dropped devlog_sections when the section-naming paragraph was removed. Added it back in the 'Before starting any task' bullet. Unrelated: running 'cargo test --test mcp' in parallel hangs several e2e subprocess tests on this box — '--test-threads=1' runs the full 136 in ~1.3s. Pre-existing, reproduces on master.
- 2 | 2026-04-26 17:33:09: RCA on Claude Code MCP 'unknown message ID' race: bug is 100% client-side (unfixed thru v2.1.119). SDK calls _onerror on unknown response ID, Claude Code's session manager treats all stdio errors as fatal via j.close(). Handler goes missing via cancel() on 60s timeout, _onclose() map wipe, or reconnection ID reset. Fast servers (~3ms) trigger it; slow ones (~1.2s) don't. The 500ms initialize stall remains the correct workaround. Upstream issues #50095, #40207, #51073 all still open.
- 3 | 2026-04-26 17:33:16: Added central server-side JSONL logging to devlogger-mcp. Dual-layer tracing: stderr (human-readable) + file (~/.local/share/devlogger/logs/mcp-server.<date>). Each tool call logs tool name, elapsed_ms, result class (success/tool_error/protocol_error), and PID. No entry text logged. Uses tracing-appender for non-blocking file writes. Best-effort — never crashes if dir creation fails.
- 4 | 2026-04-26 17:33:23: Added 5 new e2e tests covering RCA-identified gaps: oversized entry rejection on wire (new + update), rapid sequential calls, error-then-success recovery, and raw-stdio stdin-close mid-flight. Also added test filter passthrough to justfile — 'just test e2e_timing' now works while keeping --test-threads=1.
- 5 | 2026-04-26 17:33:28: Key research insight for upstream advocacy: RFC 9113 (HTTP/2) says 'Implementations MUST ignore and discard frames of unknown types' — the MCP SDK violates this principle by tearing down the transport on unknown response IDs. MCP spec itself says 'client MUST handle late responses to cancelled requests'. Both are strong arguments for the upstream fix: silently discard unknown-ID responses instead of calling _onerror.
- 6 | 2026-04-29 18:14:36: Fixed MCP stall by moving MCP file work off Tokio's global blocking pool to bounded dedicated workers and validating new/update input before blocking file work. Added just mcp-stall-repro-suite harness and made JSONL tool-completion logs independent of inherited RUST_LOG=warn. Verified just mcp-stall-repro-suite and just test pass.
- 7 | 2026-04-29 18:16:24: Added claude-template Rust language rule documenting the rmcp stdio foot gun: avoid putting whole MCP tool handlers behind Tokio's global spawn_blocking pool; validate before blocking work and e2e-test real stdio error/contention paths.
